---
name: 终端操作技能
description: 使用终端操作的时候触发
---

## 终端操作须知

- 当前这个项目不打算做跨平台，只在 window 和 浏览器端运行，所以终端操作的命令都是基于 Windows 的，默认使用 powershell，使用指令第一时间考虑 ps
- 比较复杂的操作还是请你写一个 makefile，并放置在项目根目录的 script 文件夹内，以便于我阅读和使用，makefile 的指令也是用 ps 来写
